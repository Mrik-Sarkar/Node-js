function tbl(n){
    for(let i=1; i<=10; i++){
        console.log(`${n} x ${i} = ${n*i}`);
    }
}

module .exports = {tbl}
// This function generates the multiplication table for a given number n
// It prints the table from 1 to 10 in the format "n x i = result"  
 

